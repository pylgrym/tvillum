

window.addEventListener( "resize", function(e) {
  var mv = document.getElementById('theviewport');
  mv.setAttribute('content','width='+window.innerWidth);

  // var a = document.getElementById('hack');
  // a.innerHTML = 'a';
  // a.innerHTML = 'M';
});
